{{- if .Values.portal.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "nexical-contracts-app.portal.fullname" . }}
  labels:
    {{- include "nexical-contracts-app.labels" . | nindent 4 }}
    {{- include "nexical-contracts-app.portal.label" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "nexical-contracts-app.portal.fullname" . }}
  minReplicas: {{ .Values.portal.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.portal.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.portal.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.portal.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.portal.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.portal.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
